<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EveLens.Avalonia.Views.CharacterMonitor"
             x:Class="EveLens.Avalonia.Views.CharacterMonitor.CharacterMonitorView">

    <DockPanel>
        <!-- Character header docked to top -->
        <local:CharacterMonitorHeader DockPanel.Dock="Top" DataContext="{Binding}" />

        <!-- Tab area with gear button overlay -->
        <Panel>
            <!-- Loading overlay — shown briefly until ESI identity links up -->
            <Border x:Name="LoadingOverlay" IsVisible="False" ZIndex="10"
                    Background="{StaticResource EveBackgroundDarkBrush}"
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8">
                    <TextBlock x:Name="LoadingTitle" Text="Warming up the pod..."
                               FontSize="{DynamicResource EveFontHeading}" FontWeight="SemiBold"
                               Foreground="{StaticResource EveAccentPrimaryBrush}"
                               HorizontalAlignment="Center" />
                    <TextBlock x:Name="LoadingSubtext" Text="Connecting to ESI"
                               FontSize="{DynamicResource EveFontSmall}" Foreground="{StaticResource EveTextDisabledBrush}"
                               HorizontalAlignment="Center" />
                </StackPanel>
            </Border>

            <!-- Sub-tabs with compact headers -->
            <TabControl x:Name="SubTabs" TabStripPlacement="Top" Padding="0" FontSize="{DynamicResource EveFontBody}">

                <!-- Character info tabs -->
                <TabItem Header="Skills" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterSkillsView />
                </TabItem>
                <TabItem Header="Queue" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterSkillQueueView />
                </TabItem>
                <TabItem x:Name="TabClones" Header="Clones" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterClonesView />
                </TabItem>
                <TabItem x:Name="TabEmployment" Header="Employment" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterEmploymentHistoryView />
                </TabItem>
                <TabItem x:Name="TabStandings" Header="Standings" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterStandingsView />
                </TabItem>
                <TabItem x:Name="TabContacts" Header="Contacts" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterContactsView />
                </TabItem>
                <TabItem x:Name="TabMedals" Header="Medals" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterMedalsView />
                </TabItem>
                <TabItem x:Name="TabLP" Header="LP" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterLoyaltyView />
                </TabItem>

                <!-- Data tabs -->
                <TabItem x:Name="TabAssets" Header="Assets" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterAssetsView />
                </TabItem>
                <TabItem x:Name="TabOrders" Header="Orders" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterMarketOrdersView />
                </TabItem>
                <TabItem x:Name="TabContracts" Header="Contracts" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterContractsView />
                </TabItem>
                <TabItem x:Name="TabIndustry" Header="Industry" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterIndustryJobsView />
                </TabItem>
                <TabItem x:Name="TabJournal" Header="Journal" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterWalletJournalView />
                </TabItem>
                <TabItem x:Name="TabTransactions" Header="Transactions" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterWalletTransactionsView />
                </TabItem>
                <TabItem x:Name="TabMail" Header="Mail" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterMailMessagesView />
                </TabItem>
                <TabItem x:Name="TabNotify" Header="Notify" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterNotificationsView />
                </TabItem>
                <TabItem x:Name="TabKills" Header="Kills" FontSize="{DynamicResource EveFontBody}">
                    <local:CharacterKillLogView />
                </TabItem>
            </TabControl>

            <!-- Gear button — inline with tab strip at top-right -->
            <Button x:Name="EndpointGearBtn"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Background="Transparent" BorderThickness="0"
                    Padding="8,4" Margin="4,2"
                    FontSize="{DynamicResource EveFontHeading}"
                    Foreground="{StaticResource EveTextSecondaryBrush}"
                    ToolTip.Tip="Manage ESI Endpoints">
                <Button.Content>&#x2699;</Button.Content>
                <Button.Flyout>
                    <Flyout Placement="BottomEdgeAlignedRight">
                        <Border Background="{StaticResource EveBackgroundMediumBrush}"
                                BorderBrush="{StaticResource EveBorderBrush}"
                                BorderThickness="1" CornerRadius="6"
                                Padding="12" MinWidth="260" MaxHeight="420">
                            <StackPanel Spacing="6">
                                <TextBlock Text="ESI Endpoints" FontSize="{DynamicResource EveFontSubheading}" FontWeight="SemiBold"
                                           Foreground="{StaticResource EveAccentPrimaryBrush}" />
                                <TextBlock Text="Choose which data to fetch for this character."
                                           FontSize="{DynamicResource EveFontSmall}"
                                           Foreground="{StaticResource EveTextDisabledBrush}"
                                           TextWrapping="Wrap" />
                                <Separator Margin="0,2" />

                                <!-- Endpoint toggles populated in code-behind -->
                                <ScrollViewer MaxHeight="280" VerticalScrollBarVisibility="Auto">
                                    <StackPanel x:Name="EndpointToggles" Spacing="3" />
                                </ScrollViewer>

                                <Separator Margin="0,2" />
                                <StackPanel Orientation="Horizontal" Spacing="6"
                                            HorizontalAlignment="Right">
                                    <Button Content="Enable All" FontSize="{DynamicResource EveFontSmall}" Padding="10,3"
                                            CornerRadius="12" Click="OnEnableAllEndpoints" />
                                    <Button Content="Disable All" FontSize="{DynamicResource EveFontSmall}" Padding="10,3"
                                            CornerRadius="12" Click="OnDisableAllEndpoints" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </Flyout>
                </Button.Flyout>
            </Button>
        </Panel>
    </DockPanel>
</UserControl>
