<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:EveLens.Avalonia.Markup"
             x:Class="EveLens.Avalonia.Views.PlanEditor.PlanQueueListControl">

    <!-- Timeline minimap -->
    <DockPanel>
        <StackPanel x:Name="MinimapContainer" DockPanel.Dock="Top" Margin="0,0,0,4">
            <Border x:Name="MinimapBar" Height="6" CornerRadius="3"
                    Background="{StaticResource EveBackgroundMediumBrush}"
                    ClipToBounds="True">
                <StackPanel x:Name="MinimapItems" Orientation="Horizontal" />
            </Border>
            <!-- Legend chips -->
            <WrapPanel x:Name="LegendPanel" Orientation="Horizontal" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Column headers: SKILL | TIME | R | PRI | SEC | SP/HR | PRIO | LEVEL -->
        <Grid x:Name="HeaderRow" DockPanel.Dock="Top"
              ColumnDefinitions="*,100,40,75,75,55,45,60"
              Margin="18,0,8,0" Height="24">
            <TextBlock x:Name="HeaderSkill" Grid.Column="0" Text="{loc:T Plan.Skill}" FontSize="{DynamicResource EveFontCaption}"
                       Foreground="{StaticResource EveTextDisabledBrush}"
                       VerticalAlignment="Center" />
            <TextBlock x:Name="HeaderTime" Grid.Column="1" Text="{loc:T Plan.Time}" FontSize="{DynamicResource EveFontCaption}"
                       Foreground="{StaticResource EveTextDisabledBrush}"
                       HorizontalAlignment="Right" VerticalAlignment="Center" />
            <TextBlock x:Name="HeaderRank" Grid.Column="2" Text="{loc:T Plan.Rank}" FontSize="{DynamicResource EveFontCaption}"
                       Foreground="{StaticResource EveTextDisabledBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
            <TextBlock x:Name="HeaderPri" Grid.Column="3" Text="{loc:T Plan.Primary}" FontSize="{DynamicResource EveFontCaption}"
                       Foreground="{StaticResource EveTextDisabledBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
            <TextBlock x:Name="HeaderSec" Grid.Column="4" Text="{loc:T Plan.Secondary}" FontSize="{DynamicResource EveFontCaption}"
                       Foreground="{StaticResource EveTextDisabledBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
            <TextBlock x:Name="HeaderSphr" Grid.Column="5" Text="{loc:T Plan.SPPerHour}" FontSize="{DynamicResource EveFontCaption}"
                       Foreground="{StaticResource EveTextDisabledBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
            <TextBlock x:Name="HeaderPrio" Grid.Column="6" Text="{loc:T Plan.PriorityHeader}" FontSize="{DynamicResource EveFontCaption}"
                       Foreground="{StaticResource EveTextDisabledBrush}"
                       ToolTip.Tip="{loc:T PlanEditor.PriorityHeaderTip}"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
            <TextBlock x:Name="HeaderLevel" Grid.Column="7" Text="{loc:T Plan.Level}" FontSize="{DynamicResource EveFontCaption}"
                       Foreground="{StaticResource EveTextDisabledBrush}"
                       HorizontalAlignment="Right" VerticalAlignment="Center" />
        </Grid>
        <Border DockPanel.Dock="Top" Height="1"
                Background="{StaticResource EveBorderBrush}" Margin="0,0,0,2" />

        <!-- Skill queue rows -->
        <Panel>
            <ScrollViewer x:Name="QueueScroller"
                          HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="QueueList" x:FieldModifier="private"
                              HorizontalAlignment="Stretch">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
            <!-- Drag overlay: indicator line + ghost badge -->
            <Canvas x:Name="DragCanvas" IsHitTestVisible="False"
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Border x:Name="InsertIndicator" Height="2" IsVisible="False"
                        CornerRadius="1" IsHitTestVisible="False" />
            </Canvas>
            <!-- Toast notification -->
            <Border x:Name="ToastBorder" IsVisible="False" IsHitTestVisible="False"
                    HorizontalAlignment="Center" VerticalAlignment="Bottom"
                    Margin="0,0,0,16" CornerRadius="8" Padding="18,7">
                <TextBlock x:Name="ToastText" FontSize="{DynamicResource EveFontSmall}"
                           FontWeight="Medium" Foreground="White" />
            </Border>
        </Panel>
    </DockPanel>
</UserControl>
